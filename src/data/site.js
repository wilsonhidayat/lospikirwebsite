/**
 * Site-wide content and links.
 *
 * The values live in site.json so they can be edited in the CMS (/admin)
 * without touching code. This file just re-exports them under the name the
 * components already import.
 */
import site from './site.json'

export { site }
